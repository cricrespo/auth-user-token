{% extends 'base.html.twig' %}

{% block body %}
    {{parent()}}
    <h1>Role</h1>
asdasd
    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ role.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('roles_index') }}">Back to the list</a>
        </li>
    </ul>

{% endblock %}
